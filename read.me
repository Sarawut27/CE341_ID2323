- ตอนแรก sudo apt update -y เพื่ออัพเดท
- ทำการ sudo apt install apache2 เพื่อลงโปรแกรม
- แล้วจะติดตั้งgit // sudo apt install git
- จากนั้น git init เพื่อทำให้มันเชื่อมหรือรองรับกัน
- จะ copy link เพื่อโครน repository จาก githup // git clone https://github.com/Sarawut27/CE341_ID2323.git
- แล้วจะเช็คไฟล์ที่อยู่ในโฟเดอร์ว่ามีอะไรอยู้ในนั้นบ้าง โดยใช้คำสั่ง // ls
- แล้วก็จะ cat index.html มา ดูว่าใน html ว่ามีอะไรบ้าง


